import 'package:flutter/material.dart';

class AppColor {
  static const primary = Color(0xff7b02ab);
  static const textPrimary = Color(0xff3f015b);
  static const secondary = Color(0xfff8d323);
  static const bg = Colors.white;
  // static const bg = Color.fromARGB(255, 187, 204, 252);
  static const chart = Color(0xffebbcfd);
}
